### Contributing (also useful for deployment)

```
git clone git@github.com:ogoudey/VLA_Star.git
cd VLA_Star
ssh-keygen -t ed25519 -C "email@address.com"
cat ~/.ssh/id_ed25519.pub
# send that to me
```
